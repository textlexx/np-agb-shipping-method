<?php

namespace NpAgbShippingMethod;

class WPFiltersCenter{
	
	//-----------------------------------------------------------------------
	// Example
	/*
	public static function addFilterExample():?bool{
		
		// Return default value if not exists option
		$return_default = 0;
		
		if(get_option('needed_option_example', $return_default) != 1) return false;
		if(!preg_match('#^/wp-admin#', $_SERVER['REQUEST_URI'])) return false;
		
		add_filter('example_filter_name', ClassName::$example, 10);

		return null;
	}
	*/
	//-----------------------------------------------------------------------
}

